/** @type {import('next').NextConfig} */
const nextConfig = {
  turbopack: {
    // Configuración vacía para silenciar la advertencia
    // pdfkit funcionará sin configuración especial en Turbopack
  }
};

export default nextConfig;